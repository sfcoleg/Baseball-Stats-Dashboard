"""NBA-specific presentation. Shared chrome (headers, stat tables) still
comes from app/style.py; this file is empty for now on purpose — the
skeleton has nothing custom enough yet to need it (no last-game card, no
bracket, nothing NFL's SB/conference marks needed). Add to it as real
NBA-only components come up, following app/nfl/style.py's shape."""
